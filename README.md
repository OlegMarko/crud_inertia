## Laravel + Inertia + Vue

```
    git clone git@github.com:OlegMarko/crud_inertia.git
    cd crud_inertia
    cp .env-example .env
    composer install
    ./vendor/bin/sauil up -d
    ./vendor/bin/sauil artisan test
    ./vendor/bin/sauil npm install
    ./vendor/bin/sauil npm run test
```
